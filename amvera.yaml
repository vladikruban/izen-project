meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  steps:
    - name: install
      command: npm install
    - name: build
      command: npm run build
run:
  command: npm start